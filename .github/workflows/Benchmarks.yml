name: Benchmarks
on:
  pull_request_target:
    branches: [main]
  workflow_dispatch:
permissions:
  pull-requests: write
jobs:
  bench:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        version:
          - "1"
        os:
          - ubuntu-latest
        arch:
          - x64
    steps:
      - uses: MilesCranmer/AirspeedVelocity.jl@action-v1
        with:
          julia-version: ${{ matrix.version }}
          rev: ${{ format('{0},{1}', github.event.repository.default_branch, github.event.pull_request.head.sha || github.sha) }}
          bench-on: ${{ github.event.pull_request.head.sha || github.sha }}
